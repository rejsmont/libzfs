# Example configuration for ZFS Backup Daemon
#
# This file targets the test pool created by create_test_pool.sh.
# Run the script first:  sudo ./create_test_pool.sh

# Global settings
snapshot_prefix: "autosnap"  # Prefix for all managed snapshots
check_interval: "5m"          # How often to check if snapshots are needed
dry_run: false                # Set to true to test without creating/destroying snapshots

# Datasets to backup
datasets:
  # OS root — non-recursive, hourly snapshots, long retention
  - name: "libzfs_test/ROOT/macos"
    enabled: true
    frequency: "1h"
    recursive: false
    retention:
      "1d": "1M"              # Snapshots 1 day old: keep for 1 month
      "1w": "3M"              # Snapshots 1 week old: keep for 3 months
      "1M": "1y"              # Snapshots 1 month old: keep for 1 year

  # Home directories — recursive to capture per-user datasets
  - name: "libzfs_test/USERDATA/home"
    enabled: true
    frequency: "15m"          # Snapshot every 15 minutes
    recursive: true           # Captures alice, bob, bob/projects
    retention:
      "1h": "1d"
      "1d": "1w"
      "1w": "1M"
      "1M": "1y"

  # Postgres data — frequent snapshots, long archive retention
  - name: "libzfs_test/SRVDATA/postgres"
    enabled: true
    frequency: "5m"
    recursive: true           # Captures both data and wal
    retention:
      "15m": "1d"
      "1d": "1w"
      "1w": "1M"
      "1M": "10y"

  # Media archive — daily snapshots only
  - name: "libzfs_test/USERDATA/media"
    enabled: true
    frequency: "1d"
    recursive: true           # Captures photos and videos
    retention:
      "1w": "3M"
      "1M": "10y"

  # LXD containers — disabled example
  - name: "libzfs_test/SRVDATA/lxd"
    enabled: false
    frequency: "1h"
    recursive: true
    retention:
      "1d": "1w"

# Time format reference:
# h = hours   (e.g., "1h", "12h")
# d = days    (e.g., "1d", "7d")
# w = weeks   (e.g., "1w", "2w")
# M = months  (e.g., "1M", "6M") - approximate as 30 days
# y = years   (e.g., "1y", "10y") - approximate as 365 days
#
# Retention policy logic:
# - Snapshots are matched against retention rules based on their age
# - Each rule specifies: "snapshot age": "how long to keep"
# - Example: "1d": "1M" means "snapshots that are 1 day old should be kept for 1 month"
# - A snapshot expires when: (snapshot age) + (keep duration) > now
# - Use multiple rules to create a tiered retention strategy